# src/ — application source code

**Intentionally empty.**

No code will be placed here until the plans in [`../docs/`](../docs/) have been approved.
The folder structure inside `src/` will be created as part of the technical plan
(`docs/02-technical-plan.md`), so that it matches whatever design you sign off on.
